import type { Metadata } from "next";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: {
    default: "Clear Litter — Clear & Crystal Cat Litter Reviews and Guides",
    template: "%s | Clear Litter",
  },
  description:
    "Independent reviews, buying guides, and daily articles about clear, crystal, and low-tracking cat litter.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="flex min-h-screen flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
